.DS_Store

# Built packages belong on GitHub Releases, not in git history: they are
# large, undiffable, and every version would live in the repo forever.
dist/

# Raw screen captures. Only the trimmed demo-*.mp4 files beside index.html
# are part of the site.
recordings/
